# django-htmx-views

Reusable Django app with HTMX-aware view mixins and an optional linked/chained-select endpoint.

This package separates the `apps/htmx_views` implementation from `gb119/labman` into a standalone distribution.
